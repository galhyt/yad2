{
    "type": "node",
    "scripts": {
        "client-build": "cd client && npm run build",
        "start": "npm run client-build"
    },
    "dependencies": {
        "express-pino-logger": "^4.0.0",
        "mongodb": "^3.5.6",
        "react-scripts": "^3.4.1"
    }
}
